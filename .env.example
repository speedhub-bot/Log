# ── Telegram credentials ────────────────────────────────────
BOT_TOKEN=               # From @BotFather
API_ID=                  # From my.telegram.org
API_HASH=                # From my.telegram.org
ADMIN_ID=                # Your Telegram numeric user ID

# ── Processing ──────────────────────────────────────────────
MAX_CONCURRENT_JOBS=2    # Max parallel extractions
FREE_DAILY_LIMIT_GB=2    # Free user daily quota in GB
MIN_FREE_DISK_GB=1       # Disk kept free after download/extraction work
EXTRACTION_DISK_MULTIPLIER=2.0  # Estimated temp disk needed vs archive size
RESCAN_MAX_ARCHIVE_GB=2  # Only cache archives up to this size for quick rescan

# ── Extraction ──────────────────────────────────────────────
MAX_DOMAINS_PER_EXTRACT=10  # Max domains a user can request per /extract

# ── Paths ───────────────────────────────────────────────────
DATABASE_PATH=bot.db     # SQLite database path
LOG_FILE=bot.log         # Log file path
TEMP_DIR=/tmp/cookiebot  # Temp extraction directory
